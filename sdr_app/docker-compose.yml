services:
  sdr-backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=sqlite:///./sdr_app.db
      - SECRET_KEY=change-me-in-prod
    restart: unless-stopped
